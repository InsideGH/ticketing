module.exports = {
  // Trouble with hot reload and getting Next to detect changes, try uncomment the below.
  //   webpackDevMiddleware: (config) => {
  //     config.watchOptions.poll = 300;
  //     return config;
  //   },
};
